stock-dashboard/
├── node_modules/
├── .env                  <-- NEW
├── db.js                 <-- NEW
├── package.json
├── server.js             <-- UPDATED
├── index.html
├── models/
│   └── User.js           <-- NEW
└── routes/
    └── auth.js           <-- NEW (Will be fully implemented in Step 2)






    <!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Pro Stock Trading Dashboard</title>

<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.3/dist/chart.umd.min.js"></script>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

<style>
:root {
    --bg: #0f172a;
    --card: rgba(255,255,255,0.06);
    --border: rgba(255,255,255,0.12);
    --text: #e5e7eb;
    --muted: #94a3b8;
    --green: #22c55e;
    --red: #ef4444;
    --blue: #38bdf8;
    --yellow: #facc15;
}

* {
    box-sizing: border-box;
    font-family: 'Inter', sans-serif;
}

body {
    margin: 0;
    background: radial-gradient(circle at top, #020617, #0f172a);
    color: var(--text);
}

.hidden { display: none; }

.container {
    max-width: 1400px;
    margin: auto;
    padding: 24px;
}

/* ---------- Header ---------- */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
}

.header h1 {
    font-size: 1.6rem;
    font-weight: 600;
}

#connection-status {
    padding: 8px 14px;
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 500;
}

/* ---------- Auth ---------- */
.auth-card {
    max-width: 420px;
    margin: 80px auto;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 24px;
    backdrop-filter: blur(10px);
}

.auth-card h2 {
    margin-bottom: 16px;
    text-align: center;
}

.auth-form input {
    width: 100%;
    padding: 12px;
    margin-bottom: 12px;
    background: transparent;
    border: 1px solid var(--border);
    border-radius: 10px;
    color: var(--text);
}

.auth-form button {
    width: 100%;
    padding: 12px;
    border-radius: 10px;
    border: none;
    background: var(--blue);
    font-weight: 600;
    cursor: pointer;
}

.auth-toggle {
    margin-top: 12px;
    text-align: center;
    font-size: 0.85rem;
    color: var(--muted);
    cursor: pointer;
}

/* ---------- Dashboard Grid ---------- */
.grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 20px;
}

.card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 16px;
    backdrop-filter: blur(10px);
}

.card h3 {
    margin-bottom: 12px;
    font-size: 1rem;
}

/* ---------- Stock Cards ---------- */
.stock-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit,minmax(220px,1fr));
    gap: 16px;
}

.stock-card {
    background: rgba(255,255,255,0.04);
    border-radius: 14px;
    padding: 14px;
    border: 1px solid var(--border);
}

.price-text {
    font-size: 1.2rem;
    font-weight: 600;
}

.trading-controls {
    display: flex;
    gap: 6px;
    margin-top: 8px;
}

.trading-controls input {
    width: 50px;
    padding: 6px;
    border-radius: 8px;
    border: 1px solid var(--border);
    background: transparent;
    color: var(--text);
}

.trade-btn {
    flex: 1;
    border-radius: 8px;
    border: none;
    cursor: pointer;
    font-size: 0.8rem;
}

.buy-btn { background: var(--green); }
.sell-btn { background: var(--red); }
.sub-btn { width: 100%; margin-top: 6px; background: var(--yellow); color: #000; }

/* ---------- Portfolio ---------- */
.stat {
    display: flex;
    justify-content: space-between;
    font-size: 0.9rem;
    margin-bottom: 6px;
}

table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 10px;
    font-size: 0.85rem;
}

th, td {
    padding: 8px;
    border-bottom: 1px solid var(--border);
}

/* ---------- Chart ---------- */
.chart-box {
    height: 360px;
}

/* ---------- Alerts ---------- */
#alert-notification {
    position: fixed;
    top: 20px;
    left: 50%;
    transform: translateX(-50%);
    background: var(--red);
    padding: 14px 20px;
    border-radius: 12px;
    display: none;
    z-index: 999;
}

/* ---------- Colors ---------- */
.up { color: var(--green); }
.down { color: var(--red); }

/* ---------- Responsive ---------- */
@media(max-width: 900px){
    .grid {
        grid-template-columns: 1fr;
    }
}
</style>
</head>

<body onload="checkAuth()">

<div id="alert-notification"></div>

<div class="container">

    <div class="header">
        <h1>📈 Pro Stock Dashboard</h1>
        <div id="connection-status">Disconnected</div>
    </div>

    <!-- AUTH -->
    <div id="auth-section" class="auth-card">
        <div id="login-form">
            <h2>Sign In</h2>
            <form class="auth-form" onsubmit="loginUser(event)">
                <input type="email" id="login-email" placeholder="Email" required />
                <input type="password" id="login-password" placeholder="Password" required />
                <button>Login</button>
            </form>
            <div class="auth-toggle" onclick="toggleAuthForm('register')">Create an account</div>
        </div>

        <div id="register-form" class="hidden">
            <h2>Create Account</h2>
            <form class="auth-form" onsubmit="registerUser(event)">
                <input type="email" id="register-email" placeholder="Email" required />
                <input type="password" id="register-password" placeholder="Password" required />
                <button>Create</button>
            </form>
            <div class="auth-toggle" onclick="toggleAuthForm('login')">Already registered?</div>
        </div>
    </div>

    <!-- DASHBOARD -->
    <div id="dashboard-section" class="hidden">

        <div class="grid">

            <div class="card">
                <h3>Market & Trading</h3>
                <div id="stock-list" class="stock-grid"></div>
            </div>

            <div class="card">
                <h3>Portfolio</h3>
                <div class="stat"><span>Cash</span><span id="cash-balance">$--</span></div>
                <div class="stat"><span>Holdings</span><span id="portfolio-value">$--</span></div>
                <div class="stat"><span>P&L</span><span id="total-pl-value">$--</span></div>
                <div class="stat"><span>Equity</span><span id="total-equity">$--</span></div>

                <h4 style="margin-top:12px;">Holdings</h4>
                <table>
                    <thead>
                        <tr><th>Ticker</th><th>Qty</th><th>Avg</th><th>P&L</th></tr>
                    </thead>
                    <tbody id="portfolio-table-body"></tbody>
                </table>
            </div>

        </div>

        <div class="card" style="margin-top:20px;">
            <h3>Live Price Chart</h3>
            <div class="chart-box">
                <canvas id="livePriceChart"></canvas>
            </div>
        </div>

    </div>

</div>
